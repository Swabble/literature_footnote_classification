Eine Fußnote erscheint in mehreren Literaturangaben. Wähle den Eintrag, zu dem sie tatsächlich gehört. Gib ausschließlich ein JSON-Objekt im Format {"<footnote_key>": "<entry_key>"} zurück.

Es dürfen keine Erklärungen, kein zusätzlicher Text, keine Formatierungen oder Codeblöcke enthalten sein – nur das reine JSON-Objekt.

Hier ist der Kontext: